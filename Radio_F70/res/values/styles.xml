<resources xmlns:android="http://schemas.android.com/apk/res/android">


    <style name="collection_button_copy">
        <item name="android:layout_weight">1</item>
        <!-- <item name="android:button">@null</item> -->
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/txt_gray</item>
        <item name="android:textSize">@dimen/btn_collection_text_size</item>
        <item name="android:background">@drawable/btn_collection_normal</item>
        <item name="android:layout_marginRight">@dimen/collection_button_margin_right</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="collection_button">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">0dp</item>
        <!-- <item name="android:button">@null</item> -->
        <item name="android:enabled">false</item>
        <item name="android:clickable">true</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginRight">@dimen/collection_button_margin_right</item>
        <item name="android:background">@drawable/btn_collect_bg</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/btn_collection_text_size</item>
    </style>

    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        
      <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        
      <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        
        <!-- <item name="android:windowIsTranslucent">true</item> -->
        <!-- 半透明 -->
        
     <!--    <item name="android:windowFullscreen">true</item>  -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        
		<item name="android:windowBackground">@android:color/transparent</item> 
        <!-- 背景透明 -->
        
  		 <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
        
    </style>

</resources>